import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function ToEat() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Where to Eat</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the "To Eat" component.</p>
      </CardContent>
    </Card>
  );
}
