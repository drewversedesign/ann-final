import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function ToStay() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Where to Stay</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the "To Stay" component.</p>
      </CardContent>
    </Card>
  );
}
