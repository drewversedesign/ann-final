import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function ToVisit() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>What to Visit</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the "To Visit" component.</p>
      </CardContent>
    </Card>
  );
}
