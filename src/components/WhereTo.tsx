import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function WhereTo() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Where To?</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the "Where To" component.</p>
      </CardContent>
    </Card>
  );
}
